<head>
    <meta charset="utf-8">
    <title>CSSの練習</title>
    <!--変更した部分は以下の一行！！-->
    <link rel="stylesheet" type="text/css" href="stylesheets/index.css">
    <link href="https://fonts.googleapis.com/css2?family=Zen+Maru+Gothic&display=swap" rel="stylesheet">
</head>


<div class="top-wrapper">
  <%= image_tag "IMG_2030.jpeg", class: "top-image" %>
  <div class="overlay-box">
    <div class="zemi logo">
    <%= image_tag "oikawa_logo_transparent.png", class: "top-logo" %>
    </div>
    <div class="overlay-text">
      <br><br>
      <p class="line1">社会科</p>
      <p class="line2">公共市民学専修</p><br><br>
      <p class="line3">及川ゼミ</p>
    </div>
  </div>
</div>


<div class="sticky-note">
  <h1>Recent News</h1>
</div>
<div class="letters-toptable">
    <div class="section-toplines">
    <h1>新着情報</h1>
    <%= image_tag "書く png.png", class: "topletter-image" %>
      <div class="topline mark-line"></div>
    </div>
<% if @letters.any? %>  <!-- 複数件表示 -->
  <% @letters.each do |letter| %>
    <%= link_to letter_path(letter), class: "letter-link" do %>
      <div class="letter">
        <h3><%= letter.name %></h3>  <!-- 投稿者名など -->
      </div>
    <% end %>
  <% end %>
<% else %>
  <p>まだ投稿がありません。</p>
<% end %>
</div>

<div class="toptweets">
  <div class="tweets-note">
    <h1>Recent Blogs</h1>
  </div>

  <div class="tweets-topcontainer">
    <% @tweets.last(3).reverse.each do |tweet| %>
      <%= link_to tweet_path(tweet.id), class: "tweet-link" do %>
        <div class="tweet">
          <% if tweet.image.attached? %>
            <%= image_tag tweet.image, size: '250x200' %>
          <% end %>
          <h3><%= tweet.name %></h3>
          <p><%= tweet.day %></p>
        </div>
      <% end %>
    <% end %>
  </div>
</div>