<h1 class="page-title">早稲田大学教育学部 社会科公共市民学専修 及川ゼミ</h1>
<h3 class="page-subtitle">年間予定 新規登録</h3>

<section class="plan-new-container">
  <article class="form-card">
    <% if @plan.errors.any? %>
      <div class="form-errors" role="alert">
        <strong><%= @plan.errors.count %>件のエラーがあります。</strong>
        <ul>
          <% @plan.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= form_for @plan, html: { class: "plan-form" } do |f| %>
      <div class="form-grid">
        <div class="form-field">
          <%= f.label :name, "項目名", class: "form-label" %>
          <%= f.text_field :name, class: "input-text", placeholder: "例）春学期キックオフ／合宿" %>
        </div>

        <div class="form-field">
          <%= f.label :about, "内容（詳細）", class: "form-label" %>
          <%= f.text_area :about, class: "input-textarea", rows: 6, placeholder: "日程の補足・担当・持ち物など" %>
        </div>

        <div class="form-field">
          <%= f.label :image, "画像", class: "form-label" %>
          <%= f.file_field :image, class: "input-file" %>
          <p class="help-text">※ JPG/PNG 推奨</p>
        </div>
      </div>

      <div class="form-actions">
        <%= f.submit "投稿する", class: "btn btn-primary" %>
        <%= link_to "一覧に戻る", plans_path, class: "btn btn-ghost" %>
      </div>
    <% end %>
  </article>
</section>
